# PSP Host dependencies
# 系统前置（无法用 pip 打包，需系统包提供）:
#   GStreamer + 插件 + gobject-introspection (gstreamer1.0-*, python3-gi)
#   xdotool / ydotool（输入注入回退，按需）
#
# 以下为可 pip 安装的纯 Python 依赖，已随项目内置 venv (host/venv) 一起分发，
# 运行时无需再 pip install。如需在新机器重建 venv，执行 ./setup.sh 即可。

# 运行时依赖（虚拟鼠标 uinput 设备）
evdev

# 开发 / 测试依赖
pytest