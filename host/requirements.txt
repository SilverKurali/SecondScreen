# PSP Host dependencies
# Install GStreamer first:
#   sudo apt install gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0

# Python packages (PyGObject is system-installed via apt)
# python-xlib is optional — only needed for Windows input injection fallback
# xdotool is used for Linux input injection (install via apt)

# For development / testing:
pytest